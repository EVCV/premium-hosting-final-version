---
interface SecurityBadge {
  name: string;
  description: string;
  icon: string;
  verified?: boolean;
}

interface Props {
  badges?: SecurityBadge[];
  title?: string;
  subtitle?: string;
  layout?: 'grid' | 'inline';
  className?: string;
}

const {
  badges = [
    {
      name: "SSL Certificate",
      description: "256-bit encryption for all websites",
      icon: "🔒",
      verified: true
    },
    {
      name: "GDPR Compliant",
      description: "Full compliance with EU data protection",
      icon: "🛡️",
      verified: true
    },
    {
      name: "SOC 2 Certified",
      description: "Enterprise-grade security standards",
      icon: "✅",
      verified: true
    },
    {
      name: "DDoS Protection",
      description: "Advanced threat detection and mitigation",
      icon: "🔥",
      verified: true
    },
    {
      name: "ISO 27001",
      description: "International security management standard",
      icon: "🏆",
      verified: true
    },
    {
      name: "PCI DSS",
      description: "Payment card industry security compliance",
      icon: "💳",
      verified: true
    }
  ],
  title = "Enterprise Security & Compliance",
  subtitle = "Your data is protected with industry-leading security measures",
  layout = 'grid',
  className = ""
} = Astro.props;
---

<section class={`py-16 bg-white ${className}`}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12">
      <h2 class="text-4xl font-bold text-dark mb-4">{title}</h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto">{subtitle}</p>
    </div>

    {layout === 'grid' ? (
      <!-- Grid Layout -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {badges.map((badge) => (
          <div class="luxury-card p-6 text-center group hover:shadow-lg transition-all duration-300">
            <div class="w-16 h-16 bg-gold/10 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:bg-gold/20 transition-colors duration-300">
              <span class="text-3xl">{badge.icon}</span>
            </div>
            
            <div class="flex items-center justify-center mb-2">
              <h3 class="text-lg font-semibold text-dark">{badge.name}</h3>
              {badge.verified && (
                <span class="ml-2 text-green-500" title="Verified">✓</span>
              )}
            </div>
            
            <p class="text-gray-600 text-sm leading-relaxed">{badge.description}</p>
          </div>
        ))}
      </div>
    ) : (
      <!-- Inline Layout -->
      <div class="flex flex-wrap justify-center items-center gap-8">
        {badges.map((badge) => (
          <div class="flex items-center luxury-card px-6 py-4 hover:shadow-lg transition-all duration-300">
            <span class="text-2xl mr-3">{badge.icon}</span>
            <div>
              <div class="flex items-center">
                <span class="font-medium text-dark">{badge.name}</span>
                {badge.verified && (
                  <span class="ml-2 text-green-500 text-sm">✓</span>
                )}
              </div>
              <span class="text-sm text-gray-600">{badge.description}</span>
            </div>
          </div>
        ))}
      </div>
    )}

    <!-- Trust Message -->
    <div class="text-center mt-12">
      <div class="inline-flex items-center px-6 py-3 bg-green-50 border border-green-200 rounded-full">
        <span class="text-green-500 mr-2">🛡️</span>
        <span class="text-green-800 font-medium">Your security is our top priority</span>
      </div>
    </div>
  </div>
</section>